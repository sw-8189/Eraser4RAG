[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: requires model artifacts or multiple pipeline components
    gpu: requires a CUDA-capable environment
    slow: takes materially longer than a unit test
